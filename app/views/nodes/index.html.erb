<h1>Welcome!</h1>

<h2>List of Projects</h2>



<ul id="projects">

<% @nodes.each do |node| %>

<li> <%=  link_to node.topic, node_path(node), class: 'projects_index', id: "node_#{node.id}" %> </li>

<% end %>

</ul>

<%= link_to "Create New Project", new_node_path, class: "new_project" %>